# printer.device/PBothReady



   NAME
	PBothReady - wait until output is finished

   FUNCTION
	The function PBothReady should only be called from drivers. It waits
	until the output to the printer is finished.

