/* Source: ADCD 2.1
 * Section: running-a-program
 * Library: amiga-mail
 * ADCD reference: amiga-mail/running-a-program.md
 */

        /* for compatibility */
        /* system exit special case - taken only if they have played */
        /* around with the input stream */
        if (SYSTEM && NOTSCRIPT) break;
    } while (ch != ENDSTREAMCH);
