# SDL2 AmigaOS3 Test Registry -- single source of truth
# Format: NAME CATEGORY TIER [DEPS]
#   NAME:     binary name (without path)
#   CATEGORY: SMOKE, UNIT, VIDEO, INPUT, AUDIO
#   TIER:     1 = vamos only, 2 = FS-UAE only, 12 = both, M = manual (build only)
#   DEPS:     (empty)=SDL only, TEST=+libSDL2_test, UTILS=+testutils.o
#
# Source lookup: examples/upstream/NAME.c first, then examples/NAME.c
# Parsed by: Makefile (native-examples, test-vamos), test-fsemu.sh

# --- Custom AmigaOS 3.x tests ---
test_bare      SMOKE  12
test_init      SMOKE  12
test_spinlock  UNIT   12
test_timer     UNIT   12
test_video     VIDEO  2
test_events    INPUT  2
test_sprite    VIDEO  2
test_uint64    UNIT   12
test_audio     AUDIO  12
test_loopwave  AUDIO  2
test_threads   UNIT   2
test_bmpview   VIDEO  2
test_gameloop  VIDEO  2
# test_render: SDL_RenderCopyEx rotation crashes (bus error in SW renderer)
# TODO: investigate -- may be 68k alignment issue in SDL_rotate.c
test_render    VIDEO  M

# --- Upstream SDL2 tests (from libsdl-org/SDL test/, SDL2 branch) ---
# Auto tests: self-terminating, safe for automated harness
# testsem: requires argv[1] init_value, uses SemWaitTimeout (not yet implemented)
testsem               UNIT   M
# testfile: libnix fopen("wb") allows reads (platform behavior difference)
testfile              UNIT   M
# testatomic: epic countdown test does 20M Forbid/Permit cycles, crashes (bus error)
# The basic atomic tests pass; only the 2-thread stress test at scale crashes.
testatomic            UNIT   M    TEST
testerror             UNIT   2    TEST
testplatform          UNIT   12   TEST
testqsort             UNIT   12   TEST
testver               UNIT   12   TEST
# testthread: creates thread "Two" then exits without WaitThread (raise SIGTERM).
# On AmigaOS, abandoned thread's DOS cleanup races with next test -> Guru.
# TODO: SDL_Quit should join/kill abandoned threads on AmigaOS.
testthread            UNIT   M    TEST
testtimer             UNIT   2    TEST
testfilesystem        UNIT   12   TEST

# Interactive tests: open a window/event loop, or run indefinitely.
# Built by `make examples` but skipped by automated test harness.
# Run individually: make test-fsemu TEST=checkkeys
testlock              UNIT   M
torturethread         UNIT   M
testdrawchessboard    VIDEO  M
testspriteminimal     VIDEO  M    UTILS
testmultiaudio        AUDIO  M    UTILS
checkkeys             INPUT  M    TEST
testdraw2             VIDEO  M    TEST
testsprite2           VIDEO  M    TEST,UTILS
testwm2               VIDEO  M    TEST
